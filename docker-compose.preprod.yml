name: flepourtous-preprod

services:
  api:
    container_name: flepourtous-backend-preprod
    build:
      context: ./backend
      target: preprod
    env_file:
      - ./backend/.env
    volumes:
      - ./logs:/var/www/html/logs
    command: apache2-foreground
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.api-preprod.rule=Host(`api.preprod.flepourtous.fr`)"
      - "traefik.http.routers.api-preprod.entrypoints=web,websecure"
      - "traefik.http.routers.api-preprod.tls.certresolver=le"
      - "traefik.http.services.api-preprod.loadbalancer.server.port=80"
      - "traefik.docker.network=web"
    networks:
      - web
      - internal

  app:
    container_name: flepourtous-frontend-preprod
    build:
      context: ./frontend
      dockerfile: dockerfile
      target: runner
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
        NEXT_PUBLIC_STRIPE_PUBLIC_KEY: ${NEXT_PUBLIC_STRIPE_PUBLIC_KEY}
    environment:
      - INTERNAL_API_URL=http://flepourtous-backend-preprod
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRES_IN=1h
    command: pnpm run start
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.app-preprod.rule=Host(`preprod.flepourtous.fr`)"
      - "traefik.http.routers.app-preprod.entrypoints=web,websecure"
      - "traefik.http.routers.app-preprod.tls.certresolver=le"
      - "traefik.http.services.app-preprod.loadbalancer.server.port=3000"
      - "traefik.docker.network=web"
    networks:
      - web
      - internal

networks:
  web:
    external: true
    name: web
  internal:
    internal: true
